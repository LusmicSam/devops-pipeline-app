apiVersion: v1
kind: Service
metadata:
  name: devops-pipeline-app-service
spec:
  selector:
    app: devops-pipeline-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
